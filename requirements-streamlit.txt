# Streamlit App Requirements - Cloud Deployment
# Minimal dependencies for demo app (simulated scoring)

# Streamlit framework
streamlit>=1.29.0

# Data processing (lightweight)
numpy>=1.23.0
pandas>=1.4.0

# Note: PyTorch and Transformers are NOT needed for the demo version
# The app uses simulated scoring based on essay characteristics
# This keeps deployment fast and free on Streamlit Cloud
